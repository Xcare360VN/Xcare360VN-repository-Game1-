{
  "name": "how-many-are-like-you-game",
  "version": "1.0.0",
  "private": true,
  "description": "A simple static project that uses importmaps, so no npm dependencies are needed.",
  "scripts": {
    "start": "echo \"No start script needed for this static project.\" && exit 0"
  },
  "dependencies": {}
}
